# Base64 of the je-portal-api service-account JSON key (same key reused from
# the BigQuery era - needs Sheets API scope + Viewer access on the sheet).
#   Linux/macOS : base64 -w0 .secrets/je-portal-api.json
#   PowerShell  : [Convert]::ToBase64String([IO.File]::ReadAllBytes(".secrets/je-portal-api.json"))
GCP_SERVICE_ACCOUNT_B64=

# The manually-maintained JE campaign daily-reporting sheet.
SHEETS_SPREADSHEET_ID=1AxUmSZ9MD3MuV_lEu7hksByw1AR0ZelT3ldLqscM1bA
